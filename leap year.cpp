#include<stdio.h>
int main()
{
   int a;
   printf("enter the  year a",a);
   scanf("%d",&a);
   if(a%4==0)
   printf("the given year is leap year");
   else
   printf("the given year is non leap year");
   return 0;
}
